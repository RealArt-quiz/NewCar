let cars=[];fetch('cars.json').then(r=>r.json()).then(d=>{cars=d;loadCars()});function loadCars(){let r=[...cars];let min=+minPrice.value||0,max=+maxPrice.value||9999999,range=+minRange.value||0;if(fuel.value!='Alle')r=r.filter(x=>x.fuel==fuel.value);r=r.filter(x=>x.price>=min&&x.price<=max&&x.range>=range);results.innerHTML=r.map(c=>`<div class=car><h3>${c.brand} ${c.model}</h3><p>${c.fuel}</p><p>${c.price.toLocaleString('da-DK')} kr.</p><p>Rækkevidde ${c.range} km</p><a target=_blank href='${c.website}'>Hjemmeside</a> | <a target=_blank href='https://www.google.com/search?q=${encodeURIComponent(c.brand+" "+c.model+" FDM test")}'>FDM</a> | <a target=_blank href='https://www.google.com/search?q=${encodeURIComponent(c.brand+" "+c.model+" Bil Magasinet test")}'>Bil Magasinet</a></div>`).join('')}
